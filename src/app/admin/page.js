'use client';import Shell from '@/components/Shell';import {getFeedbacks,getStatuses} from '@/lib/store';export default function Admin(){const f=getFeedbacks(),s=Object.values(getStatuses());return <Shell admin><div className="pageHead"><div><p className="eyebrow">ADMIN</p><h1>검토 Dashboard</h1><p className="muted">전체 SME 검토 진행 상황을 확인합니다.</p></div></div><div className="stats"><div><b>90</b><span>전체 직무</span></div><div><b>{s.filter(x=>x.status==='검토완료').length}</b><span>검토 완료</span></div><div><b>{f.length}</b><span>등록 Feedback</span></div><div><b>{new Set(f.map(x=>x.user_id)).size}</b><span>참여 SME</span></div></div><section className="card"><h2>최근 Feedback</h2>{f.slice(-8).reverse().map((x,i)=><p key={i}><span className="tag">{x.feedback_type}</span> {x.review_result} · {x.user_id}</p>)}{!f.length&&<p className="muted">아직 등록된 의견이 없습니다.</p>}</section></Shell>}
