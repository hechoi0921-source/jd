'use client'
import {useState} from 'react';import {useRouter} from 'next/navigation'
export default function Login(){const r=useRouter(),[email,setEmail]=useState(''),[pw,setPw]=useState(''),[error,setError]=useState('');function submit(e){e.preventDefault();if(email==='admin@jd.local'&&pw==='0123'){localStorage.setItem('jd_user',JSON.stringify({email,role:'admin',name:'관리자'}));r.push('/admin')}else if(email&&pw){localStorage.setItem('jd_user',JSON.stringify({email,role:'sme',name:email.split('@')[0]}));r.push('/review')}else setError('이메일과 비밀번호를 입력해 주세요.')}return <main className="login"><section className="loginCard"><div className="brandMark">JD</div><p className="eyebrow">SEOYON GROUP</p><h1>직무정보 SME 검토</h1><p className="muted">직무 정의부터 Skill까지 한 화면에서 검토합니다.</p><form onSubmit={submit}><label>이메일<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@company.com"/></label><label>비밀번호<input type="password" value={pw} onChange={e=>setPw(e.target.value)} placeholder="비밀번호"/></label>{error&&<p className="error">{error}</p>}<button>로그인</button></form><p className="hint">관리자 데모 계정: admin@jd.local</p></section></main>}
